var INIT = {};// localStorage index : filenameINIT.files = {	"json-obs-catalog":'obs-txt-1-obs-catalog.json',	"json-obs-en":'obs-txt-1-en-obs-en.json',	"json-obs-es":'obs-txt-1-es-obs-es.json',	"json-obs-ru":'obs-txt-1-ru-obs-ru.json',	"json-obs-fr":'obs-txt-1-fr-obs-fr.json',	"json-obs-pt-br":'obs-txt-1-pt-br-obs-pt-br.json'};$(document).ready(function(){	if ( isMobileDevice() ) {        document.addEventListener("deviceready", INIT.onDeviceReady, false);    } else {        INIT.onDeviceReady();    }	    });INIT.onDeviceReady = function(){	if (localStorage.getItem("initial_setup") === null || localStorage.getItem("initial_setup") == "0"){				localStorage.setItem('initial_setup', '0');		var key;		for(key in INIT.files) {			console.log(key);				$.ajax({									dataType: "json",				url: 'assets/json/'+INIT.files[key],				async: false,				success: function(json){					console.log(json);					localStorage.setItem(key, JSON.stringify(json));				} // success								}); //ajax			}		localStorage.setItem('initial_setup', '1');		window.location.hash = HASH.myDefault;		$(window).trigger("hashchange");			}};